{
  "name": "datacenter-mapping-frontend",
  "version": "1.0.0",
  "description": "Frontend for DataCenter Interactive Mapping System",
  "type": "module",
  "scripts": {
    "test": "vitest run --config vitest.config.js",
    "test:watch": "vitest --config vitest.config.js",
    "test:integration": "playwright test",
    "test:all": "npm run test && npm run test:integration"
  },
  "keywords": [
    "datacenter",
    "mapping",
    "leaflet",
    "geospatial"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@vitest/ui": "^1.0.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}